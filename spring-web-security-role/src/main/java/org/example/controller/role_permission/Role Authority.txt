TODO. 授权用户特定的角色和权限
Collection<? extends GrantedAuthority> grantedAuthorities = authentication.getAuthorities();

.roles("ADMIN") === .authorities("ROLE_ADMIN")
.roles("USER")  != .authorities("USER")  自动为角色添加ROLE_前缀
.authorities("READ", "WRITE", "DELETE")  不做任何处理，直接使用授权名称(READ, WRITE等)

isAuthenticated() && hasAuthority('ROLE_ADMIN') && hasAuthority('READ') 使用方式